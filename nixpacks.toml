# Nixpacks configuration for ASP.NET Core BlogApp

[phases.build]
cmds = ["dotnet publish -c Release -o out"]

[start]
cmd = "dotnet out/BlogApp.dll"

[variables]
ASPNETCORE_URLS = "http://+:3000"
ASPNETCORE_ENVIRONMENT = "Production"
